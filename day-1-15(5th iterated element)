#include <stdio.h>

int main() {
    int array[] = {1, 2, 7, 4, 8, 6, 3, 5, 9, 10};
    int size = sizeof(array) / sizeof(array[0]);
    printf("192373024_Akhilkumar\n");
    for (int i = 0; i < size; i++) {
        if (i == 4) {
            printf("5th Iterated Element: %d", array[i]);
            break;
        }
    }
    
    return 0;
}
